version: '3.8'

# Einfache Docker Compose Konfiguration für lokales Testen
# Start mit: docker-compose -f docker-compose.local.yml up -d

services:
  weather-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lesence-weather-app
    restart: unless-stopped
    ports:
      - "3000:80"

